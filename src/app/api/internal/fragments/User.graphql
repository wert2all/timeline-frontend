fragment ShortTimeline on ShortUserTimeline {
  id
  name
}

fragment User on User {
  id
  name
  email
  avatar
  timelines {
    ...ShortTimeline
  }
  accounts {
    ...Account
  }
}

fragment Account on Account {
  name
  avatar
}
